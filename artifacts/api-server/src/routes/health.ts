import { Router, type IRouter } from "express";
import { HealthCheckResponse } from "@workspace/api-zod";

const router: IRouter = Router();

export const APP_VERSION = "0.1.71";
export const APP_BUILD_TIME = "2026-04-23";
// Changelog: keep only the 10 most recent versions; drop the oldest entry when adding a new one.
export const APP_CHANGELOG = [
  "v0.1.71 (2026-04-23): SDK 指纹与模型元数据维护 —— ① Deno 运行时版本 2.7.12→2.7.13（openai-sdk-deno preset 的 user-agent 与 x-stainless-runtime-version 同步）；② LiteLLM 版本 1.83.11→1.83.12（litellm preset user-agent 与 desc 同步）；③ 模型上下文窗口校正：mistral-large-latest 128K→256K（Mistral Large 3 官方 256K），mistral-small-latest 128K→262K（Mistral Small 4 官方 262,144 tokens）；以上更新均已通过 PyPI / GitHub Releases / Mistral 官方文档实时核查；其余 SDK 指纹（openai JS 6.34.0、openai Python 2.32.0、@anthropic-ai/sdk 0.90.0、anthropic Python 0.96.0、@google/genai 1.50.1、google-genai 1.73.1、httpx 0.28.1、requests 2.33.1、Vercel AI SDK 6.0.168、Bun 1.3.13、curl 8.19.0、Chrome 147）经实时核查均为当前最新，无需变更；版本号同步：前端 LOCAL_VERSION/LOCAL_BUILD_TIME、后端 APP_VERSION/APP_BUILD_TIME、docs/maintenance-guide.md 一并升至 0.1.71",
  "v0.1.70 (2026-04-22): UsageLogsPage 三处前端微调，全部贴合 UI_DESIGN.md 与 replit.md 已声明的硬约束 —— ① 表头列宽改自适应：原 <colgroup> 13 列固定 px（120/150/100/110/50/70/75/75/75/75/80/100/70 = 1150px）+ <table minWidth:1150px> 在窄于 1150px 的视口（如画布 871px iframe）会触发外层 overflowX:auto 横向滚动条，用户必须鼠标滑动才能看到「伪装/操作」两列；本次将 13 个 <col> 的 width 全部按原 1150px 等比换算为百分比（10.43%/13.04%/8.70%/9.57%/4.35%/6.09%/6.52%×4/6.96%/8.70%/6.08%，Σ≈100%），同时移除 <table> 的 minWidth:1150px，tableLayout:fixed 继续保留（满足 v0.1.67 引入、v0.1.69 重申的「表格不得回退到 auto 布局」硬约束），效果是表格随 Card 容器宽度等比缩放、列宽比例锁定不抖动、常规桌面视口不再出现横向滚动条；overflowX:auto 仍保留作为极窄视口（< ~600px）兜底；② 成功率徽标从「使用日志」标题右侧的 absolute (left:100%, marginLeft:10px, pointerEvents:none) 浮层迁入下方统计卡片行，与「总请求/成功/失败/总Tokens/估算费用」共用同一 7px 14px padding + 8px 圆角 + 14px 数字 + 12px 标签 + 90px minWidth 的卡片样式，颜色仍按 ≥95%/≥80%/否则 三档绿/黄/红分级；位置精确插入「失败」与「总 Tokens」之间，从根本上消除 v0.1.68 起在窄视口下徽标穿模 Proxy Key/Admin Key 输入框的 z-order 冲突；③ 同步前后端版本号至 0.1.70（前端 LOCAL_VERSION/LOCAL_BUILD_TIME、后端 APP_VERSION/APP_BUILD_TIME、docs/api-passthrough.md、replit.md）；本次仅 UsageLogsPage 表格 colgroup + 统计卡片排序与版本号文本变更，未触碰 ModelsPage/PROVIDER_LABELS 顺序、circle 实线 SVG、/v1beta/models 与 /v1/models 文案、UI 三档字号 22/18/14（次级 12，禁用 11/13/13.5）、SegmentedControl 等任何已声明设计约束；④ 后续追加（同版本号下的小改）：UsageLogsPage colgroup 列宽按用户多轮指定收敛为 模型 18 / 时间 14 / 供应商·费用 各 10 / 端点·类型·状态·用时·首字·输入·输出·伪装 各 6（12 列合计 100%，操作列由 min-content 承载剩余空间），同步抬升表格主要内容字号 12→14px（model badge / 用时 / 首字 / 输入 / 输出 / 费用 / BillingPage 模型分布数字列）、保持次级元数据 12px；UsageLogsPage 操作列由原本三个常驻按钮（# 复制 ID / ↗ 跳转实时日志 / ▶ 重放）重构为右键弹出菜单（onContextMenu 阻止浏览器默认菜单 + fixed 浮层 + 自动避开右下边缘 + Esc/点空白/滚动/resize 自动关闭），同步删除 .usage-jump-btn CSS 与「操作」列表头/列宽占位；OverviewPage「API 端点」从 6 条扩展为 8 条，补齐 GET /v1beta/models 与 GET /v1beta/models/{model}，并将 /v1/models 描述与 /v1beta/models 措辞统一为「列出可用模型（…格式）」以满足 replit.md 第 41 行硬约束；前端结构性优化首批落地：新增 src/lib/apiFetch.ts（统一 baseUrl 拼接 + Bearer 注入 + 默认 8s AbortSignal.timeout + ApiError 包装）、src/hooks/useLocalStorageState.ts（带跨 tab storage 事件同步的通用 LS hook）、src/hooks/useAuthKeys.ts（useAdminKey / useProxyKey 包装 sessionStorage 读写，setter 自动 persist），App.tsx 全部 fetch 调用迁移至 apiFetch / apiFetchJson、densityMode 改用 useLocalStorageState、adminKey/proxyKey 改用 useAdminKey/useProxyKey、expandedGroups 初始值由 PROVIDER_LABELS keys 派生（新增 provider 单点维护），SettingsPage / LogsPage / BillingPage / UsageLogsPage 移除原本散落在每个 onChange 里的 writeAdminKey / writeProxyKey 显式调用（持久化下沉至 useAuthKeys），消除多处「setAdminKey + writeAdminKey 成对调用」的冗余与不一致；本批改动不修改任何 AI 请求路径、不增删任何 /api/ /v1/ /v1beta/ 端点、不变更请求体内容（中转纯净度硬约束保持），不触碰 ModelsPage 顺序、circle 实线 SVG、UI 三档字号 22/18/14（次级 12，禁用 11/13/13.5）、SegmentedControl、tableLayout:fixed、PROVIDER_LABELS 顺序与 OpenRouter 尾部例外、健康检查 15 分钟节奏（用户明确指定）等任何已声明硬约束；本次微改未触发版本号增长，仅在 v0.1.70 changelog 内追加本段记录；⑤ 后续追加：DocsPage「API 端点详解」八条端点顺序按 OverviewPage 排列重排（GET /v1/models → GET /v1beta/models → GET /v1beta/models/{model} → POST /v1/chat/completions → POST /v1/responses → POST /v1/messages → POST /v1beta/models/{model}:generateContent → POST /v1beta/models/{model}:streamGenerateContent），同步把两条 Gemini POST 路径占位符从 `:model:` 形式改为 Google 官方 `{model}` 形式与 OverviewPage / replit.md 完全一致；新增 components/TabNav.tsx 把 App.tsx 第 171-192 行行内标签导航（22 行）抽出为独立组件，App.tsx 主体由 207 行降至 ~185 行，逻辑等价、视觉零变化",
  "v0.1.69 (2026-04-21): 修复使用日志空态时表头列宽撑开造成的「时间/模型」间距视觉错位 —— v0.1.67 把表格改为 tableLayout:fixed + <colgroup> 锁列宽（时间 120 / 模型 180 / 供应商 100 / 端点 110 ...）后，没有数据时表头依然按完整 1180px 列宽渲染，左对齐表头之间出现 90~150px 不均匀空隙（最宽的 180px 模型列表头与右侧供应商之间空隙最大），看起来像「时间和模型栏不间距对齐」；本次将空态从 <tr><td colSpan=13> 改为 Card 内独立 <div>（48px 上下内边距 + 居中），仅在 usageLogs.length>0 时渲染 <table>+<thead>+<tbody>+<colgroup> 与外层 overflowX:auto 滚动容器，从根本上避免空态出现宽列骨架；底部「显示 X 条 / 共 Y 条」原本已经条件渲染，无需改动；硬约束「表格不得回退到 auto 布局」继续保持（有数据时仍是 fixed + colgroup）；版本号同步至 0.1.69",
  "v0.1.68 (2026-04-21): 修复使用日志统计与筛选不一致 —— UsageLogsPage 此前 stats 卡片（总请求/成功/失败/总Tokens）与「成功率」徽标始终读取后端 /api/usage-logs 返回的 GLOBAL stats（服务端单次反向遍历环形缓冲区时全量累加，忽略 model/provider/status 查询参数），与表格仅展示已过滤行的行为冲突；当用户筛选 SiliconFlow 等无匹配数据的供应商时，表格显示「暂无使用日志」但顶部仍显示「5 总请求 / 80% 成功率」，造成「整体界面布局/数据出问题」的误解；本次客户端派生：新增 hasFilter = !!(model||provider||status)，无筛选时仍用服务端 globalStats（保留环形缓冲区超 200 行的全量信息），有筛选时改用 useMemo 从可见 usageLogs 即时累加 totalRequests/successCount/errorCount/totalTokens，与表格底部「显示 X 条 / 共 Y 条」（Y 仍为全局 total）形成「过滤视图 + 全局上下文」的清晰组合；服务端 /api/usage-logs 不动（其 stats 字段语义保持「全局」），改动仅在前端 UsageLogsPage 第 85-104 行；版本号同步：前端 LOCAL_VERSION、后端 APP_VERSION、docs/api-passthrough.md、replit.md 一并升至 0.1.68",
  "v0.1.67 (2026-04-21): UI 设计规范二次审查与修复 —— 新增通用 components/SegmentedControl.tsx（支持 size sm/md、allowDeselect、每项 accentColor + badge，圆角 8/6、14px 标签 + 12px 徽标），统一替换使用日志「趋势/性能/伪装」与实时日志「全部/INFO/WARN/ERROR」筛选切换，原 usageLogs/ToggleButton 降级为兼容性再导出；TrendPanel 重写：图表高度 64→96px、顶部 stats 改为 4 列 grid 卡片（14px 数值 + 12px 标签 + 8px 圆角 + LegendDot 同色点）、坐标轴改 12px Menlo，整体可读性显著提升；PerformancePanel 顶部 SegmentedControl + 修复「按模型」竖排 bug（wordBreak: break-all → whiteSpace: nowrap）；UsageLogsPage 表格改 tableLayout: fixed + <colgroup> 显式锁定 13 列宽度（时间 120 / 模型 180 / 供应商 100 / 端点 110 / 类型 50 / 状态 70 / 用时·首字·输入·输出 各 75 / 费用 80 / 伪装 100 / 操作 70）+ min-width 1180px，解决筛选 OpenAI/全部/Anthropic 时列宽抖动；LogsPage 删除空态书本 emoji 📋 与连接前 ○ 图标（顺带消除超规 28px 字号），INFO 徽标多轮迭代后回退至软填充胶囊（12% 语义色背景 + 30% 同色边 + 6px 圆角 + sans-serif 700 + 0.6px 字距）；ReferencePage 模型路由规则完全按 PROVIDERS 顺序重排（OpenAI→Anthropic→Google→OpenRouter→xAI→DeepSeek→Mistral→Moonshot→Groq→Cerebras→Together→SiliconFlow→Fireworks→Novita→Hyperbolic）并补齐 Cerebras/Fireworks/Novita/Hyperbolic 4 条规则共 15 行；DocsPage 模型信息重组：仅保留 OpenAI/Anthropic/Google 三大独立章节 + OpenRouter（Google 之后）+ 原生与 OpenAI 兼容通道（OpenRouter 之后），原 DeepSeek 独立章节并入兼容通道首句保留 platform.deepseek.com 关键说明；产出 artifacts/api-portal/docs/UI_AUDIT_v0.1.66.md 审查报告，replit.md 加入 SegmentedControl 与 fixed table 硬约束；版本号同步：前端 LOCAL_VERSION、后端 APP_VERSION、docs/api-passthrough.md、replit.md 一并升至 0.1.67",
  "v0.1.66 (2026-04-21): 前端 UI 一致性收尾 —— LogsPage 连接按钮去除 ⚡ 闪电图标，从实心 #0ea5e9 蓝改为靛蓝幽灵风格 (rgba(99,102,241,0.15) 底 + rgba(99,102,241,0.3) 边 + #a5b4fc 字)，与导出/清空/刷新按钮统一为低饱和半透明语义色；空状态合并 !adminKey 分支，统一为「○ 点击「连接」开始接收实时日志」；UsageLogsPage 全部供应商下拉静态 5 项替换为基于 PROVIDER_LABELS 的 15 项动态映射，新增供应商后只需改 models.ts 单点；BillingPage Admin Key 输入框 / 币种 select / 刷新按钮从 12px 5px 6px-radius 升至 14px 6×10 7px-radius，与前两 Tab 完全一致；新增 components/Dropdown.tsx 自定义下拉组件（永远朝下展开、点外面/Esc 关闭、深底 + 靛蓝高亮选中项）替换原生 <select>，解决小 iframe 视口里下拉反向上弹问题，覆盖使用日志的供应商/状态过滤与费用统计的币种选择；PROVIDER_LABELS 顺序按模型列表页真实顺序对齐 (OpenAI→Anthropic→Google→OpenRouter→xAI→DeepSeek→Mistral→Moonshot→Groq→Cerebras→Together→SiliconFlow→Fireworks→Novita→Hyperbolic)，AppHeader 横幅同步；SettingsPage 5 个「确认修改/保存」按钮从填入 adminKey 后变实心 #6366f1 + 白字改为同款靛蓝幽灵 (修改 Admin Key/Proxy Key 确认 ×2、预算保存、供应商保存、伪装应用)，禁用态保留 #6366f1 暗色 + 0.4 opacity；UsageLogsPage 表格 tableLayout 由 fixed 改 auto 仅模型列 maxWidth 180px ellipsis，消除横向滚动；URL 参数同步 ?tab= 覆盖全部 8 个 Tab，监听 popstate 支持浏览器前进后退；版本号同步：前端 LOCAL_VERSION、后端 APP_VERSION、docs/api-passthrough.md、replit.md 一并升至 0.1.66",
  "v0.1.65 (2026-04-21): 修复 OpenRouter 预估费用为空的问题 —— 前端 lookupPricing 之前对 owner/model 形式的 OR 镜像模型（如 openai/gpt-5、deepseek/deepseek-r1、x-ai/grok-4、moonshotai/kimi-k2 等）查不到价格，因为对应直连厂商的价表项是 EXACT_PRICING（精确键 \"gpt-5\" 等），无通用 prefix 规则可命中 includes()，导致 UsageLogsPage 的成本列显示 \"—\"；新增 owner 命名空间剥离兜底：在 EXACT/PREFIX 全表均未命中后，若 model 含 \"/\"，截取 \"/\" 之后的子串再查 EXACT 与 PREFIX 一次（owner-prefixed OR-specific 规则保持优先级最高，不影响已正确命中的 anthropic/qwen/cohere/perplexity 等）；并向 PREFIX_PRICING OR 区块补齐 deepseek/deepseek-r1|v3|v3.1|v3.2|chat 与 meta-llama/llama-3.3-70b、llama-3.2-90b-vision 几条直连规则未覆盖的 OR 镜像价；后端 billing.ts 因 PRICING_TABLE 已用 includes() 匹配 prefix 风格 match 字符串，对 OR owner/model 形态本就能正确穿透至直连价规则，无需修改",
  "v0.1.64 (2026-04-21): React Query 迁移收尾与代码清理 —— BillingPage 与 UsageLogsPage 完成 useQuery/useMutation 迁移后，移除 UsageLogsPage 过滤下拉框（provider/status）onChange 中的 setTimeout(fetchUsageLogs, 50) 兜底重拉（queryKey 已包含过滤项，filter 变更自动触发新查询，setTimeout 为旧手动状态机时代的遗留代码）；BillingPage 同步删除 useCallback/useRef 未用导入与 hasFetchedRef/prevAdminKeyRef/lastFetched 状态机；review 后确认 LogsPage 不迁移决策正确（sinceIndex 增量轮询 + 用户控制连接/断开 与 useQuery 不匹配）；版本号同步：前端 LOCAL_VERSION、后端 APP_VERSION、docs/api-passthrough.md、replit.md 一并升至 0.1.64",
  "v0.1.63 (2026-04-21): OpenRouter 模型上下文窗口实时同步 —— 后端 ModelRecord 新增可选 contextLength；fetchOpenAIStyleModels 在 OpenRouter 分支解析 context_length（顶层字段优先，回退 top_provider.context_length）；/api/models 与 /v1/models 响应在该字段已知时附加 context_length（额外字段，不破坏 OpenAI 兼容）；前端 useLiveOpenRouterModels 读取并格式化为 K/M，写入 ModelEntry.context，ModelsPage OpenRouter 分组的上下文徽标直接展示；前端引入 React Query：main.tsx 挂载 QueryClientProvider（默认 staleTime 60s / gcTime 5min / refetchOnWindowFocus false / retry 1），useLiveOpenRouterModels 由模块级缓存改为 useQuery（queryKey [openrouter-models, baseUrl]），ModelsPage + DocsPage 共用同一缓存避免双重请求；React Query 进一步扩展到 BillingPage（fetchBilling → useQuery [billing/usage, baseUrl, currency, adminKey]，enabled 仅在 billing 标签页 + 有 adminKey 时，staleTime 25s 略低于服务端 30s 缓存；saveBudget → useMutation，onSuccess 延迟 350ms invalidateQueries 绕过缓存；移除 hasFetchedRef/prevAdminKeyRef/lastFetched 手工状态机，currency/adminKey 经 queryKey 自动重拉）和 UsageLogsPage（fetchUsageLogs → useQuery [usage-logs, baseUrl, adminKey, model, provider, status]，过滤项变更自动重拉；refetchInterval 仅在 usageAutoRefresh && activeTab=usage 时为 USAGE_AUTO_REFRESH_INTERVAL 15min【禁止修改】，替代 usageIntervalRef + setInterval；openReplay/sendOneRequest 一次性请求保持原样不迁移）；不迁移 LogsPage（其 sinceIndex 增量轮询 + 用户控制连接/断开 与 useQuery 纯函数式 queryFn 不匹配，强行迁移会改变可见行为且无收益）；顺带修复 ModelsPage STATIC_GROUPS 类型 Record<Provider> → Record<Exclude<Provider, openrouter>>（openrouter 改由 useLiveOpenRouterModels 注入后该静态映射本就不应包含 openrouter 键）；版本号同步：前端 LOCAL_VERSION、后端 APP_VERSION、docs/api-passthrough.md 与 replit.md 保持 0.1.63，本次新增功能并入同一版本号未单独升号",
  "v0.1.62 (2026-04-21): OpenRouter 列表完全实时化 —— 移除前后端 OPENROUTER_FEATURED_MODELS / OPENROUTER_MODELS 静态数组（清空为 []），改由后端 /api/models 端点定时（60s 缓存）拉取 openrouter.ai/api/v1/models 并返回；前端新增 useLiveOpenRouterModels(baseUrl) Hook，ModelsPage 与 DocsPage 的 OpenRouter 模型面板（计数、服务商分组、搜索）全部基于实时数据，端点公开可读、不破坏中转纯净度；模型元数据按官方页面校正：OpenAI GPT-5 系列(gpt-5/5.1/5.2/mini/nano/5.2-codex/5.3-codex)上下文 128K→400K，Anthropic claude-opus-4-5* 1M→200K，xAI grok-4 2M→256K（grok-4-fast 仍 2M），Google gemini-3-pro-image-preview 与 gemini-2.5-flash-image 1M→64K、gemini-3.1-flash-image-preview 1M→128K；proxy.ts Anthropic /v1/models 过滤剔除 -thinking / -thinking-visible 虚拟别名，与 gemini-native.ts 行为一致；TS2345 修复：v0.1.61 引入的 gemini-native.ts GET /v1beta/models/:model 中 req.params.model 类型 string|string[] 适配；Node 依赖升级（minor/patch）：vite 8.0.8→8.0.9、tailwindcss 4.2.1→4.2.2、@tailwindcss/vite 4.2.1→4.2.2、@google/genai 1.48→1.50.1、@tanstack/react-query 5.90.21→5.99.2、@types/node 25.3.5→25.6.0、@types/pg 8.18→8.20、framer-motion 12.35.1→12.38.0、openai 6.33→6.34、orval 8.5.3→8.8.0、drizzle-kit 0.31.9→0.31.10、@anthropic-ai/sdk 0.78→0.90.0；保留 chokidar 4 / p-retry 7（major 跨版本破坏性更新本轮不动）；SDK 指纹（Bun 1.3.13、Deno 2.7.12、curl 8.19.0、Chrome 147、Python 端 openai/anthropic/google-genai/litellm/httpx/requests）复核全部当前最新",
];

router.get("/healthz", (_req, res) => {
  const data = HealthCheckResponse.parse({ status: "ok" });
  res.json(data);
});

router.get("/version", (_req, res) => {
  res.setHeader("Access-Control-Allow-Origin", "*");
  res.setHeader("Access-Control-Allow-Methods", "GET");
  res.json({
    version: APP_VERSION,
    buildTime: APP_BUILD_TIME,
    changelog: APP_CHANGELOG,
  });
});

export default router;
